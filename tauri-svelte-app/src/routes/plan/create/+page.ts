export const ssr = false;

/*
import { error } from '@sveltejs/kit';
type IPostItem = {
  id: number,
  title : string,
  content : string,
  category : string,
  createdAt: string,
}

export async function load({ params }) {
    return {
    };  
}
*/
